# The API answers on its own domain, so a production build must call it by absolute URL.
# Baked into the bundle at build time — not a secret; it is visible in the shipped JS either
# way. Host only: the client appends /api.
VITE_API_URL=https://crmapi.ssbpgc.com
